import { Google } from "../../pages/googlePage";
// const loginUser = { email: 'test', password: '1231' };

describe('User login', () => {

    beforeEach(() => {
        Google.mainPage.open();
    });

    // it.only ; xit

    it('C1 - User should be able to login', () => {
    });
});